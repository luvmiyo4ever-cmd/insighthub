runtime:
  ragMode: fixture
  llmProvider: fixture
  embeddingProvider: fixture
  llmModel: ""
  embeddingModel: ""

postgres:
  enabled: true

redis:
  enabled: true

secretsStore:
  enabled: false

ingress:
  enabled: false

api:
  replicaCount: 1
  autoscaling:
    enabled: false

worker:
  replicaCount: 1

web:
  replicaCount: 1
  autoscaling:
    enabled: false
